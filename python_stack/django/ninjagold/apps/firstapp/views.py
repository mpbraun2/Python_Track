# -*- coding: utf-8 -*-
from django.shortcuts import render, redirect

import random

def index(request):
    

    if sessiongold = 0
    return render(request, 'firstapp/index.html')

    if request.POST['building'] == 'Farm'
        sessiongold =+ random.randint(10,20)
    return redirect(/)
#if click on Cave, random 5-10 gold, update session gold
#if click on house, random 2-5 gold, update session gold
#if click on Casino, random -50-50 gold, update session gold.

def process(request):

